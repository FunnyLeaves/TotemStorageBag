# AGENTS.md

本文件是仓库内所有 Coding Agent（含本会话）必须遵守的执行准则。当与其他指令冲突时，以更具体、更新鲜的指令为准。

## 项目概述

- 项目名称：TotemStorageBag，目标游戏为《逃离鸭科夫》（Escape from Duckov，Steam AppID 3167020）。
- Mod 项目：TotemStorageBag（图腾收纳包，游戏内道具名“图腾卷轴”）：8 格图腾容器，工作台用“任意三个Ⅲ级图腾 + 20 虚化的羽毛 + 2000 合成费”制作。
- 当前状态：实现与调试阶段；技术栈为 VS 2022 + .NET Standard 2.1 + Harmony 2.4.2；当前功能、关键路径与构建部署流程以 [DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md) 为准，改动后需同步更新该文档。

## Agent 行为总则

1. 回答保持客观，基于证据和实际代码给出结论，不迎合用户。
2. 新建或生成文件前，先说明文件的大致内容、作用以及所属模块或功能分支。
3. 文件命名可更改时（即非用户明确指定），先询问用户是否更改名称，不擅自改名。
4. 保持目录简洁：不堆砌无关文件，能合并、能复用现有结构时优先复用；删除或移动文件前说明原因。

## 开源项目开发准则

1. 启动新项目或制定较大方案前，优先到 GitHub 检索同类项目、文档、实现思路和源码，作为设计与实现依据。
2. 存在成熟现成项目时，先评估 license、维护状态、安全风险和适配成本，再决定复用、借鉴或自研。
3. 简单 bug 修复、明确的小改动或离线任务，可不强制调研。
4. 多检索、多拆解学习开源项目：可复用内容直接取用，有参考价值的方案借鉴其优点。
5. 多套开源方案交叉对比，取优势、剔除缺陷，并在必要时记录选择理由。

## 文档同步规则

1. 功能开发、业务逻辑、接口协议、数据结构、产品交互或模块职责发生变化时，必须同步检查相关 README、开发规范、接口文档和当前状态文档是否需要更新。
2. 文档同步属于开发收尾的一部分：在说明"完成"、运行 test 通过或 commit 之前，都要检查文档是否过期。
3. 如果只是内部实现细节调整，且不影响使用方式、接口、数据、交互和模块边界，可以说明"无需更新文档"。
4. 如果旧文档会误导后续 Agent 或人工开发者，必须更新或归档，不能让过期文档继续留在默认阅读路径。
5. 文档更新优先更新当前执行入口和模块 README；历史设计、历史计划类文档如已归档，只在需要保留背景时补充归档说明，不作为当前执行依据。
6. 更新当前执行文档时，优先把正文改成当前真实规则，不要用"修订说明""当前改为""之前如何"这类补丁式语气堆在顶部或正文里。
7. 只有归档文档、migration note、changelog 或用户明确要求保留历史脉络时，才记录历史修订说明；当前执行文档应保持像正式说明书，而不是更新日志。

## 提交与收尾要求

- 每次对外说明"完成"、提交代码或合并分支前，先按"文档同步规则"逐条核对，确认文档未过期。
- 提交前检查变更范围，不夹带无关文件；涉及文件新建、改名、删除时确认是否符合"目录简洁"原则。
